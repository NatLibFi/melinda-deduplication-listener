{
  "name": "@natlibfi/melinda-deduplication-listener",
  "description": "Listener microservice of Melinda deduplication system",
  "homepage": "https://github.com/natlibfi/melinda-deduplication-listener-melinda",
  "bugs": {
    "url": "https://github.com/natlibfi/melinda-deduplication-listener-melinda/issues"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:natlibfi/melinda-deduplication-listener-melinda.git"
  },
  "license": "Apache-2.0",
  "version": "1.0.0",
  "engines": {
    "node": ">=7"
  },
  "main": "dist/index",
  "files": [
    "dist",
    "bin/start"
  ],
  "bin": "./bin/start",
  "scripts": {
    "prepare": "npm run build",
    "lint": "eslint --fix src",
    "clean:build": "rimraf dist",
    "build:transpile": "babel src -d dist",
    "build": "npm run clean:build && npm run build:transpile",
    "test": "npm run lint && flow",
    "test:unit": "NODE_ENV=test mocha *.spec.js --compilers js:babel-register src/**/*.spec.js",
    "test:watch": "NODE_ENV=test mocha *.spec.js --compilers js:babel-register src/**/*.spec.js -w",
    "dev": "concurrently --kill-others 'npm run build:transpile -- -w' 'sleep 5 && nodemon -w dist dist/index.js'",
    "publish-to-npm": "npm test && npm publish --access=public"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-eslint": "^7.2.3",
    "babel-plugin-module-resolver": "^3.0.0",
    "babel-preset-env": "^1.6.1",
    "babel-preset-flow": "^6.23.0",
    "concurrently": "^3.5.0",
    "eslint": "^4.1.1",
    "eslint-plugin-flowtype": "^2.34.0",
    "eslint-plugin-import": "^2.6.0",
    "eslint-import-resolver-babel-module": "^3.0.0",
    "flow-bin": "^0.48.0",
    "mocha": "^4.0.1",
    "nodemon": "^1.11.0"
  },
  "dependencies": {
    "@natlibfi/aleph-change-listener": "^1.0.0",
    "@natlibfi/melinda-deduplication-common": "^2.0.0",
    "amqplib": "^0.5.1",
    "body-parser": "^1.17.2",
    "debug": "^2.6.8",
    "es6-promisify": "^5.0.0",
    "express": "^4.15.4",
    "http-status-codes": "^1.2.0",
    "lodash": "^4.17.4",
    "marc-record-js": "^0.3.2",
    "oracledb": "^1.13.1"
  }
}
